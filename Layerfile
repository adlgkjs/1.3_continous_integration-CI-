# Use an Ubuntu 18.04 base
FROM ubuntu:18.04

# Install PHP 8.1
RUN apt-get update && \
    apt-get install -y php8.1 php8.1-cli

# Expose port 80
EXPOSE 80

# Start a simple PHP server
CMD ["php", "-S", "0.0.0.0:80"]
